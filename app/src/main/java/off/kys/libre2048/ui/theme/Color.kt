package off.kys.libre2048.ui.theme


import androidx.compose.ui.graphics.Color

/**
 * Libre2048 theme colors for light and dark schemes.
 * Following Material Design 3 color system.
 */

/*--------------*/
/*-Light Colors-*/
/*--------------*/

/** Light mode primary color. Used for key components across the UI. */
val lightPrimary = Color(0xFF0057CE)

/** Color used for text and icons displayed on top of the primary color. */
val lightOnPrimary = Color(0xFFFFFFFF)

/** A tonal color used for containers that need less emphasis than [lightPrimary]. */
val lightPrimaryContainer = Color(0xFFD8E2FF)

/** Color used for text and icons displayed on top of the primary container color. */
val lightOnPrimaryContainer = Color(0xFF001947)

/** Light mode secondary color. Used for less prominent components in the UI. */
val lightSecondary = Color(0xFF0057CE)

/** Color used for text and icons displayed on top of the secondary color. */
val lightOnSecondary = Color(0xFFFFFFFF)

/** A tonal color used for containers that need less emphasis than [lightSecondary]. */
val lightSecondaryContainer = Color(0xFFD8E2FF)

/** Color used for text and icons displayed on top of the secondary container color. */
val lightOnSecondaryContainer = Color(0xFF001947)

/** Light mode tertiary color. Used for contrasting accents. */
val lightTertiary = Color(0xFF006E17)

/** Color used for text and icons displayed on top of the tertiary color. */
val lightOnTertiary = Color(0xFFFFFFFF)

/** A tonal color used for containers that need less emphasis than [lightTertiary]. */
val lightTertiaryContainer = Color(0xFF95F990)

/** Color used for text and icons displayed on top of the tertiary container color. */
val lightOnTertiaryContainer = Color(0xFF002202)

/** Background color for the light theme. */
val lightBackground = Color(0xFFFDFBFF)

/** Color used for text and icons displayed on top of the background. */
val lightOnBackground = Color(0xFF1B1B1E)

/** Surface color for the light theme, used for components such as cards and sheets. */
val lightSurface = Color(0xFFFDFBFF)

/** Color used for text and icons displayed on top of the surface. */
val lightOnSurface = Color(0xFF1B1B1E)

/** A variant of the surface color for light theme. */
val lightSurfaceVariant = Color(0xFFE1E2EC)

/** Color used for text and icons displayed on top of the surface variant. */
val lightOnSurfaceVariant = Color(0xFF44464E)

/** Outline color used for boundaries like borders. */
val lightOutline = Color(0xFF757780)

/** Color for text/icons on top of [lightInverseSurface]. */
val lightInverseOnSurface = Color(0xFFF2F0F4)

/** Surface color that contrasts with [lightSurface], used for snack-bars. */
val lightInverseSurface = Color(0xFF303033)

/** Inverse primary color. */
val lightPrimaryInverse = Color(0xFFAEC6FF)


/*-------------*/
/*-Dark Colors-*/
/*-------------*/

/** Dark mode primary color. Used for key components across the UI. */
val darkPrimary = Color(0xFFAEC6FF)

/** Color used for text and icons displayed on top of the dark primary color. */
val darkOnPrimary = Color(0xFF002C71)

/** A tonal color used for containers in dark mode that need less emphasis than [darkPrimary]. */
val darkPrimaryContainer = Color(0xFF00419E)

/** Color used for text and icons displayed on top of the dark primary container color. */
val darkOnPrimaryContainer = Color(0xFFD8E2FF)

/** Dark mode secondary color. Used for less prominent components in the UI. */
val darkSecondary = Color(0xFFAEC6FF)

/** Color used for text and icons displayed on top of the dark secondary color. */
val darkOnSecondary = Color(0xFF002C71)

/** A tonal color used for containers in dark mode that need less emphasis than [darkSecondary]. */
val darkSecondaryContainer = Color(0xFF00419E)

/** Color used for text and icons displayed on top of the dark secondary container color. */
val darkOnSecondaryContainer = Color(0xFFD8E2FF)

/** Dark mode tertiary color. Used for contrasting accents. */
val darkTertiary = Color(0xFF7ADC77)

/** Color used for text and icons displayed on top of the dark tertiary color. */
val darkOnTertiary = Color(0xFF003907)

/** A tonal color used for containers in dark mode that need less emphasis than [darkTertiary]. */
val darkTertiaryContainer = Color(0xFF00530D)

/** Color used for text and icons displayed on top of the dark tertiary container color. */
val darkOnTertiaryContainer = Color(0xFF95F990)

/** Background color for the dark theme. */
val darkBackground = Color(0xFF1B1B1E)

/** Color used for text and icons displayed on top of the background in dark theme. */
val darkOnBackground = Color(0xFFE4E2E6)

/** Surface color for the dark theme. */
val darkSurface = Color(0xFF1B1B1E)

/** Color used for text and icons displayed on top of the surface in dark theme. */
val darkOnSurface = Color(0xFFE4E2E6)

/** A variant of the surface color for dark theme. */
val darkSurfaceVariant = Color(0xFF44464E)

/** Color used for text and icons displayed on top of the surface variant in dark theme. */
val darkOnSurfaceVariant = Color(0xFFC5C6D0)

/** Outline color used for boundaries in dark theme. */
val darkOutline = Color(0xFF8E9099)

/** Color for text/icons on top of [darkInverseSurface]. */
val darkInverseOnSurface = Color(0xFF1B1B1E)

/** Surface color that contrasts with [darkSurface] in dark theme. */
val darkInverseSurface = Color(0xFFE4E2E6)

/** Inverse primary color in dark theme. */
val darkPrimaryInverse = Color(0xFF0057CE)
